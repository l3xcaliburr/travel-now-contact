{
  "name": "travel-contact-form-processor",
  "version": "1.0.0",
  "description": "Lambda function to process travel contact form submissions",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "aws-sdk": "^2.1490.0",
    "uuid": "^9.0.1"
  }
}
